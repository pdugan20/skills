interface:
  display_name: "Code-Native UI Ideation"
  short_description: "Explore UI directions in runnable code"
  default_prompt: "Use $code-native-ui-ideation to brainstorm and compare these UI directions in the running project."
policy:
  allow_implicit_invocation: true
