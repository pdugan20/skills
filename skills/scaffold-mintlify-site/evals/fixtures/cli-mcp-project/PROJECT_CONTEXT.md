# Project context

This is a Python command-line application with a FastMCP server. The repository
contains `pyproject.toml`, `src/acme/cli.py`, `src/acme/mcp_server.py`, and an
existing root `Makefile`. It has no `docs-mintlify/`, `docs/`, or `website/`
directory. No Mintlify account, mirror repository, domain, or deployment has
been configured.
