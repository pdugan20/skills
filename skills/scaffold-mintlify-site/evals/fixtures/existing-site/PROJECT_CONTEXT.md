# Project context

The repository already contains `docs-mintlify/docs.json`, twelve MDX pages,
custom logos, and a working preview script. The navigation is cluttered and the
quickstart is incomplete. The user asks to "set up Mintlify properly" without
explicitly authorizing replacement of the existing site.
