---
title: Configure sync
description: "Choose which services sync, and how often."
icon: arrows-rotate
---

Edit `~/.acme/config.toml` to choose services and the sync interval. Run
`acme-sync run` to apply changes immediately.
