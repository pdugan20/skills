---
title: Export your library
description: "Dump your full library to JSON or CSV at any time."
icon: file-export
---

Run `acme-sync export --format json` to write the full library to stdout.
Use `--format csv` for spreadsheet-friendly output.
