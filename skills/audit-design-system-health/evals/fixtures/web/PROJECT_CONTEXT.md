# Web repository context

The Next.js portfolio uses CSS custom properties, Tailwind utilities, shadcn adapter aliases, component CSS, editorial MDX styles, bespoke data visualizations, development experiments, and repository-specific UI guard scripts. Existing work must remain untouched.
