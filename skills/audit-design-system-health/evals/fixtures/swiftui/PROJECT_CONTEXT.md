# SwiftUI repository context

The app uses theme constants, semantic colors, shared card and provider components, SwiftUI previews, a debug component gallery, accessibility scenarios, optical media-overlay values, and feature-specific data colors.
