interface:
  display_name: "Bootstrap Repository"
  short_description: "Scaffold repositories to the right maturity"
  default_prompt: "Use $bootstrap-repository to set up this new repository with proportional local and GitHub configuration."
policy:
  allow_implicit_invocation: true
