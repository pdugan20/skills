# React Native project context

The messenger uses semantic theme hooks, shared pressable and typography primitives, on-device Storybook, resettable fixture providers, and accessibility tests. The selected reaction component intentionally gives the user's own reaction stronger emphasis.
