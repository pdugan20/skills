# SwiftUI project context

The app has central theme values, semantic colors, shared media cards, SwiftUI previews, a debug component gallery, and UI accessibility scenarios. The selected media card intentionally uses separate image and caption actions plus a provider wordmark treatment.
