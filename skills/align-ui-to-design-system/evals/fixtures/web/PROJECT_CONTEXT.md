# Web project context

The portfolio uses CSS custom properties for spacing and color, shared focus styles, project-local display typography, responsive development routes, and browser checks. The selected project card intentionally uses bespoke editorial type but contains some one-off spacing and focus declarations.
