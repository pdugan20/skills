# Project context

The service commits `openapi.yaml` generated from its application routes. The
Mintlify site has hand-written endpoint pages that frequently drift. It needs an
overview page and native endpoint navigation without a custom endpoint-page
generator.
