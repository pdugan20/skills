# Project context

The repository registers FastMCP tools in four domain modules. The live server
can be imported in development, and tool schemas are available through MCP
introspection. The current hand-written `reference/mcp-tools.mdx` contains stale
tool names and counts.
