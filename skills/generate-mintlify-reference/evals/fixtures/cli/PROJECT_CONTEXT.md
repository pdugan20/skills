# Project context

The Python package exposes a Typer CLI from `acme.cli:app`. Command help is the
authoritative interface. The existing Mintlify site has a Reference tab but no
CLI pages or freshness check. Commands are organized into `sync`, `export`, and
`account` groups.
